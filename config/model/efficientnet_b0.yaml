# EfficientNet-B0 model configuration
model:
  backbone: "efficientnet_b0"
  num_classes: 4
  pretrained: true
  dropout_rate: 0.3  # Lower dropout for EfficientNet
  use_global_avg_pool: true

# Loss configuration
loss:
  type: "bce_with_logits"  # "bce_with_logits" or "focal"
  
  # For BCEWithLogitsLoss
  pos_weight: [6.25, 20.74, 0.29, 9.42]  # Based on class distribution analysis
  
  # For FocalLoss
  focal_alpha: 1.0
  focal_gamma: 2.0

# Optimizer configuration
optimizer:
  type: "adamw"
  lr: 0.0005  # Lower learning rate for EfficientNet
  weight_decay: 0.01
  betas: [0.9, 0.999]

# Learning rate scheduler
scheduler:
  type: "cosine_annealing"
  T_max: 100
  eta_min: 0.00005